<nav class="<%= yield :nav_class %>">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navigation-example">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Blog Rails', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id="navigation-example">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to instruction_path do %>
            <i class="material-icons">help_outline</i> Instruction
          <% end %>
        </li>
        <% unless @current_user %>
          <li>
            <%= link_to login_path do %>
              <i class="material-icons">fingerprint</i> Sign in
            <% end %>
          </li>
        <% end %>
        <% if @current_user %>
          <li>
            <%= link_to new_article_path do %>
              <i class="material-icons">add</i> New article
            <% end %>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <i class="material-icons">account_circle</i> <%= @current_user.username %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li class="dropdown-header">Sign in as <b><%= @current_user.username %></b></li>
              <!--<li class="divider"></li>-->
              <!--<li><a href="#">Instruction</a></li>-->
              <!--<li class="divider"></li>-->
              <li><%= link_to 'Sign out', logout_path, method: :delete %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
